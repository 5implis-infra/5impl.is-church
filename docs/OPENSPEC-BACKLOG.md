# OpenSpec ↔ Backlog.md Sync Integration

> Ferramenta [Backlog.md](https://github.com/MrLesk/Backlog.md) — gerenciador de conhecimento local-first em Markdown.
> Este projeto projeta OpenSpec changes e documentação canônica como tasks e docs no Backlog.

---

## Architecture

### Sync Direction

```
OpenSpec (canonical)  ──────→  Backlog (projection)
                              ←──────  controlled fields only
```

### Scripts

| Script | Purpose |
|--------|---------|
| `scripts/sync-openspec-change-to-backlog.ts` | Main sync: OpenSpec → Backlog tasks |
| `scripts/sync-backlog-task-to-openspec.ts` | Reverse sync: Backlog → OpenSpec |
| `scripts/backlog-sync-doc-index.ts` | Sync: `docs/` + `docs/adrs/` → `.backlog/docs/` + `.backlog/decisions/` |
| `scripts/backlog-sync-backlog-to-docs.ts` | Sync reverse: `.backlog/docs/` + `.backlog/decisions/` → `docs/` |

### Hooks

- `scripts/.git/sync-openspec-changes-to-backlog.sh` — pre-commit: OpenSpec ↔ Backlog tasks
- `scripts/.git/install-backlog-sync-hook.sh` — pre-commit: `docs/` ↔ Backlog docs/decisions

---

## Doc Index Sync (`docs/` ↔ Backlog Docs)

Além do sync de tasks, este projeto mantém um **índice de documentação** no Backlog:

```
docs/                   ← Canônico
  *.md                  → .backlog/docs/doc-<name>.md
  adrs/*.md             → .backlog/decisions/decision-###.md
```

**O que o hook faz:**

1. Ao detectar arquivos criados, modificados ou removidos em `docs/` ou `docs/adrs/`
2. Executa `backlog-sync-doc-index.ts` (idempotente, determinístico)
3. Gera stubs em `.backlog/docs/` e `.backlog/decisions/` com marker `generated-by: adponte-backlog-doc-index`
4. Remove stubs whose canonical source no longer exists

**Regra:** Canonical source é sempre `docs/` e `docs/adrs/`. Backlog docs são projeções read-only.

---

## How It Works

### OpenSpec → Backlog (Forward Sync)

When the pre-commit hook runs:

1. **Detect changes**: Scans `openspec/changes/` for active changes
2. **Generate task ID**: Increments from existing `.backlog/tasks/` files
3. **Build frontmatter**: `id`, `title`, `status`, `labels`, `references`, `documentation`
4. **Embed snapshots**: Content from `proposal.md`, `design.md`, `tasks.md`, `plan.md`, `verify.md` embedded with markers
5. **Derive status**: Based on `tasks.md` checklist completion (0%→To Do, partial→In Progress, 100%→Done)
6. **Validate**: Checks YAML frontmatter and body markers

Generated tasks are marked with `generated-by: openspec-backlog-task-sync` in frontmatter.

### Backlog → OpenSpec (Reverse Sync)

The reverse sync reads Backlog tasks and updates OpenSpec for **controlled fields only**:

- **Status changes**: Updates `.openspec.yaml`
- **Checklist progress**: Updates `tasks.md` checkbox items
- **Implementation Notes**: Appends to `notes.md` or `verify.md`

**Important**: Snapshot section content is NEVER written back to OpenSpec artifacts. This is a one-way guard.

### Archive Flow

When OpenSpec marks a change as archived (`status: archived` in `.openspec.yaml`):

1. Generates `.backlog/archive/<task-id> - <title>.md` with full snapshots
2. Removes the active `.backlog/tasks/<task-id> - <title>.md` if it was generated by sync (has marker and no manual content)
3. Preserves manually-created Backlog tasks on archive

---

## Snapshot Format

Each embedded section uses markers:

```markdown
## OpenSpec Proposal
*This section is generated from OpenSpec. Edit the OpenSpec artifact, not this snapshot.*
<!-- OPENSPEC:PROPOSAL:BEGIN -->
[content here]
<!-- OPENSPEC:PROPOSAL:END -->
```

Sections embedded:
- `PROPOSAL` - from `proposal.md`
- `DESIGN` - from `design.md`
- `TASKS` - from `tasks.md`
- `PLAN` - from `plan.md`
- `VERIFY` - from `verify.md` (if present)
- `SPECS-SUMMARY` - aggregated from `specs/**/spec.md`

---

## Ownership Model

| Tool | Role | Notes |
|------|------|-------|
| OpenSpec | Canonical source | Design, planning, decisions, specs |
| Backlog.md | Projeção operacional | Quadro Kanban diário, docs indexados |
| `docs/` + `docs/adrs/` | Canonical docs | Projeta para `.backlog/docs/` + `.backlog/decisions/` |

**Regras:**
- Artifacts OpenSpec são editados diretamente
- Backlog tasks e docs são gerados automaticamente — não editar diretamente
- Canonical source: `docs/` + `docs/adrs/` para documentação; `openspec/changes/` para tasks

---

## Troubleshooting

### Task not syncing
- Check that `proposal.md` exists in the OpenSpec change directory
- Verify the change directory is under `openspec/changes/`

### Status not updating
- Forward sync derives status from `tasks.md` checklist items
- If checklist is empty, status defaults to `To Do`

### Reverse sync not working
- Verify Backlog task has `generated-by: openspec-backlog-task-sync` marker
- Only controlled fields (status, checklist, notes) sync back

### CLI validation warning
- The sync scripts validate files directly even if Backlog CLI is unavailable
- This is expected behavior — validation still runs

---

## Files

| Path | Role |
|------|------|
| `.backlog/tasks/` | Backlog tasks gerados de OpenSpec changes |
| `.backlog/docs/` | Backlog stubs gerados de `docs/*.md` |
| `.backlog/decisions/` | Backlog stubs gerados de `docs/adrs/*.md` |
| `.backlog/archive/` | Tasks arquivadas |
| `scripts/sync-openspec-change-to-backlog.ts` | OpenSpec → Backlog tasks sync |
| `scripts/sync-backlog-task-to-openspec.ts` | Backlog → OpenSpec reverse sync |
| `scripts/backlog-sync-doc-index.ts` | `docs/` → Backlog docs sync |
| `scripts/backlog-sync-backlog-to-docs.ts` | Backlog docs → `docs/` reverse sync |
| `scripts/.git/sync-openspec-changes-to-backlog.sh` | Hook: OpenSpec ↔ Backlog tasks |
| `scripts/.git/install-backlog-sync-hook.sh` | Hook installer for doc index sync |