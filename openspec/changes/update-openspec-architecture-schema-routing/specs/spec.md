## ADDED Requirements

(None — this change modifies OpenSpec infrastructure configuration, not product behavior.)

## MODIFIED Requirements

(None — no existing capability behavior is changed.)

## REMOVED Requirements

(None — no capability behavior is removed.)