## 1. Script Infrastructure

- [x] 1.1 Create `scripts/sync-openspec-change-to-backlog.ts` — main sync script (OpenSpec → Backlog)
- [x] 1.2 Create `scripts/sync-backlog-task-to-openspec.ts` — reverse sync script (Backlog → OpenSpec controlled fields)
- [x] 1.3 Create `scripts/.git/sync-openspec-changes-to-backlog.sh` — pre-commit hook runner
- [x] 1.4 Add pre-commit hook call to `.git/hooks/pre-commit`

## 2. Backlog Task Generation

- [x] 2.1 Implement frontmatter builder (id, title, status, labels, references, documentation, priority)
- [x] 2.2 Implement snapshot embedding for proposal.md with `<!-- OPENSPEC:PROPOSAL:BEGIN -->` markers
- [x] 2.3 Implement snapshot embedding for design.md with `<!-- OPENSPEC:DESIGN:BEGIN -->` markers
- [x] 2.4 Implement snapshot embedding for specs summary (aggregated from specs/**/spec.md)
- [x] 2.5 Implement snapshot embedding for tasks.md with `<!-- OPENSPEC:TASKS:BEGIN -->` markers
- [x] 2.6 Implement snapshot embedding for plan.md with `<!-- OPENSPEC:PLAN:BEGIN -->` markers
- [x] 2.7 Implement snapshot embedding for verify.md with `<!-- OPENSPEC:VERIFY:BEGIN -->` markers
- [x] 2.8 Add regeneration header to each embedded section ("This section is generated from OpenSpec...")
- [x] 2.9 Add `generated-by: openspec-backlog-sync` marker to frontmatter

## 3. Status Mapping

- [x] 3.1 Parse OpenSpec tasks.md checklist items and calculate completion percentage
- [x] 3.2 Map 0% → `To Do`, partial → `In Progress`, 100% → `Done`
- [x] 3.3 Implement Done → archive transition trigger

## 4. Archive Projection

- [x] 4.1 Detect OpenSpec archive state (archived change directory or `.openspec.yaml` status)
- [x] 4.2 Generate `.backlog/archive/<task-id> - <title>.md` with full snapshots including verify/retrospective
- [x] 4.3 Remove active `.backlog/tasks/<task-id> - <title>.md` if generated by sync (has marker, no manual content)
- [x] 4.4 Preserve manually-created Backlog tasks on archive (skip removal if manual content detected)

## 5. Reverse Sync (Controlled Fields)

- [x] 5.1 Read Backlog task frontmatter for status changes → update `.openspec.yaml`
- [x] 5.2 Read Backlog task AC/DoD checklist → update corresponding checklist in OpenSpec tasks.md
- [x] 5.3 Read `Implementation Notes` section → append to verify.md or create notes.md in OpenSpec change
- [x] 5.4 Guard: do NOT write snapshot section content back to OpenSpec artifacts

## 6. Validation

- [x] 6.1 Validate generated Backlog task has valid YAML frontmatter
- [x] 6.2 Validate body sections contain required markers and non-empty content
- [x] 6.3 Run `backlog task list --plain` if CLI available; warn if unavailable
- [x] 6.4 Implement drift detection: log when OpenSpec content changed since last sync
- [x] 6.5 Run `openspec validate sync-openspec-changes-to-backlog-tasks --type change --strict`

## 7. Documentation

- [x] 7.1 Create `docs/OPENSPEC-BACKLOG.md` with integration overview, ownership model, and troubleshooting
- [x] 7.2 Update `AGENTS.md` with a short integration note and link to docs/OPENSPEC-BACKLOG.md

## 8. Initial Sync (Optional First Run)

- [x] 8.1 Run initial sync on existing OpenSpec changes (existing changes created before this integration)
- [x] 8.2 Verify all generated Backlog tasks are readable and have correct content