schema: spec-driven

context: |
  Language: Portuguese (pt-BR)
  All artifacts must be written in Brazilian Portuguese.

  This project uses Expo, Hono, tRPC, Prisma, Tailwindcss v4.

  We are using the monorepo architecture with Git submodules.

  The goal is to create a SaaS product for churches.

