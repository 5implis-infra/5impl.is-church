schema: intent-driven

context: |
  Language: Portuguese (pt-BR)
  All artifacts must be written in English.

  For propose/apply/verify/archive workflows, use the local `openspec-git-discipline` skill
  to enforce proposal commits.

rules:
  architecture-doc:
    - Must use arc42-documentation skill
    - May use c4-architecture skill
  brainstorm:
    - Must use brainstorming skill
  proposal:
    - Should use grill-me skill when relevant scope information is missing
  design:
    - Must use c4-architecture skill
    - Must use caveman skill
    - Must use improve-codebase-architecture skill
    # Apply only when change involves frontend/design/system:
    - Should use site-architecture skill
    - Should use tailwind-design-system skill
    - Should use frontend-design skill
    - Should use ui-ux-pro-max skill
  adr:
    - Must use architecture-decision-records skill
    - May use c4-architecture skill
  specs:
    - Must use gherkin-authoring skill
  tasks:
    - Must use openspec-apply-change skill
  verify:
    - Must use openspec-verify-change skill
